;; Generated by benchmarks/disequality/scripts/generate_snapshots.py from
;; benchmarks/disequality/inductive-prover/benchmarks/propel/gset_comm.propel,
;; imported from the Dis/Equality Graphs artifact. This is zero-based graph
;; index 42 (position 43 of 52), rendered with
;; direct constructors as an outcome-preserving chronological host replay.
; Direct host language generated from source declarations.
; source operator "@apply"/2
; source operator "@case"/2
; source operator "@match"/2
; source operator "@match"/3
; source operator "@λ"/2
; source operator "Unit"/0
; source operator "⊤"/0
; source operator "⊥"/0
(sort PropelTerm)
(constructor Atom (String) PropelTerm)
(constructor Apply (PropelTerm PropelTerm) PropelTerm)
(constructor Case (PropelTerm PropelTerm) PropelTerm)
(constructor Match2 (PropelTerm PropelTerm) PropelTerm)
(constructor Match3 (PropelTerm PropelTerm PropelTerm) PropelTerm)
(constructor Lambda (PropelTerm PropelTerm) PropelTerm)
(constructor S (PropelTerm) PropelTerm)
(constructor Escaped_Unit () PropelTerm)
(constructor Z () PropelTerm)
(constructor ≛ (PropelTerm) PropelTerm)
(constructor ≟ (PropelTerm PropelTerm) PropelTerm)
(constructor Top () PropelTerm)
(constructor Bot () PropelTerm)

; Outcome-preserving chronological host interaction replay.
; host trace counts: commands=15, rebuilds=8, stats_reads=2, clones=6.

; host command result: success
(let $term0 (Atom "a"))
(let $term1 (Atom "n₁"))
(let $term2 (Apply $term0 $term1))
(let $term3 (Top))
(let $term4 (Case $term3 $term3))
(let $term5 (Bot))
(let $term6 (Atom "n₂"))
(let $term7 (Atom "b"))
(let $term8 (Apply $term7 $term6))
(let $term9 (Atom "c"))
(let $term10 (Apply $term9 $term6))
(let $term11 (Case $term5 $term10))
(let $term12 (Match3 $term8 $term4 $term11))
(let $term13 (Lambda $term6 $term12))
(let $term14 (Apply $term13 $term1))
(let $term15 (Case $term5 $term14))
(let $term16 (Match3 $term2 $term4 $term15))
(let $term17 (Atom "n₄"))
(let $term18 (Apply $term0 $term17))
(let $term19 (Apply $term7 $term17))
(let $term20 (Case $term5 $term19))
(let $term21 (Match3 $term18 $term4 $term20))
(let $term22 (Lambda $term17 $term21))
(let $term23 (Atom "n₃"))
(let $term24 (Apply $term22 $term23))
(let $term25 (Apply $term9 $term23))
(let $term26 (Case $term5 $term25))
(let $term27 (Match3 $term24 $term4 $term26))

; host rebuild (the preceding mutation batch was flushed)

; host command result: check_failed
(fail (check (= $term16 $term27)))

; host command result: expect_fail_failed
(fail (check-known-disequal $term16 $term27))

; host graph clone

; host command result: success
(union $term1 $term23)

; host rebuild (the preceding mutation batch was flushed)

; host command result: expect_fail_failed
(fail (fail (check-disequalities )))

; host graph clone

; host command result: success
(union $term1 $term23)
(disequal $term5 $term3)
(union $term2 $term5)

; host rebuild (the preceding mutation batch was flushed)

; host command result: expect_fail_failed
(fail (fail (check-disequalities )))

; host graph clone

; host command result: success
(let $term28 (Apply $term7 $term23))
(disequal $term28 $term3)

; host rebuild (the preceding mutation batch was flushed)

; host command result: expect_fail_failed
(fail (fail (check-disequalities )))

; host graph clone

; host command result: success
(union $term1 $term23)
(let $term29 (Apply $term0 $term23))
(union $term29 $term5)
(union $term28 $term5)

; host rebuild (the preceding mutation batch was flushed)

; host command result: expect_fail_failed
(fail (fail (check-disequalities )))

; host graph clone

; host command result: success
(union $term1 $term23)
(union $term29 $term5)
(union $term28 $term5)

; host rebuild (the preceding mutation batch was flushed)

; host command result: expect_fail_failed
(fail (fail (check-disequalities )))

; host graph clone

; host command result: success
(union $term1 $term23)
(union $term29 $term5)
(union $term28 $term5)

; host rebuild (the preceding mutation batch was flushed)

; host command result: expect_fail_failed
(fail (fail (check-disequalities )))

; host rebuild (the preceding mutation batch was flushed)

; host stats read (numeric values are backend-specific and not replayable)

; host stats read (numeric values are backend-specific and not replayable)
